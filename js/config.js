/* ============================================================
   Warblers Meadery — site configuration
   Paste your Supabase project values here (Supabase dashboard →
   Project Settings → API) to switch the guestbook mural + contact
   form from single-browser demo mode to the real shared backend,
   and to activate the admin sign-in at /admin.html.

   SUPABASE_URL      → "Project URL"        e.g. "https://abcd1234.supabase.co"
   SUPABASE_ANON_KEY → "anon public" key    (SAFE to publish — it only grants
                                             what the database row-level
                                             security policies allow)
   Never put the service_role key anywhere in this repo.
   ============================================================ */
window.WARBLERS_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
